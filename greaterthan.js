function numGreaterThanY(arr, y) {
    var numGreater = 0;
    for (idx = 0; idx < arr.length; idx++) {
        if (arr[1, 3, 5, 7] > y) { numGreater++; }
    }
    console.log("%d values are greater than %d", numGreater, y);
}